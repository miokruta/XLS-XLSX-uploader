<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <mvc:annotation-driven />

    <!-- XMLs IMPORT -->
    <import resource="../database/database-queries.xml"/>
    <import resource="../database/database-properties.xml"/>

    <!-- DAOs -->
    <bean id="jdbcPostgresDAO" class="dao.JDBCPostgreDataAccessObject">
        <constructor-arg name="properties" ref="postgresProperties" />
    </bean>

    <!-- REPOSITORIES -->
    <bean id="spreadsheetFileRepo" class="repo.SpreadsheetFileRepository">
        <constructor-arg name="dao" ref="jdbcPostgresDAO" />
        <constructor-arg name="queries" ref="spreadsheetTableQueries" />
    </bean>

    <!-- CONTROLLERS -->
    <bean id="spreadsheetFilesController" class="controller.SpreadsheetFilesRestController">
        <constructor-arg name="dataTransferObjectRepository" ref="spreadsheetFileRepo" />
    </bean>

    <!-- VIEW RESOLVER -->
    <bean id="viewResolver"
          class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="viewClass" value="org.springframework.web.servlet.view.JstlView"/>
        <property name="prefix" value="/view/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="10000000"/>
    </bean>
</beans>